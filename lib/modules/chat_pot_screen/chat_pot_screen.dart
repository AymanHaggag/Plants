import 'package:flutter/material.dart';

class ChatPotScreen extends StatelessWidget {
  const ChatPotScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("start new chat"),),
    );
  }
}
